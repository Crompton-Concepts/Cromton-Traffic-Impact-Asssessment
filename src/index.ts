/**
 * TIA Bundle Entry Point
 * Re-exports all typed modules so the built bundle exposes them on `window.TIA`.
 */

export * from './types';
export * as ApiClient from './api-client';
export * as UI from './ui-utils';
