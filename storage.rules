rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // {path=**} matches nested folders (datasets/QLD/..., datasets/VIC/..., etc.)
    match /datasets/{path=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
